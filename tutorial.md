# 《Categorical Deep Learning is an Algebraic Theory of All Architectures》
## 第2.3节--第3节 翻译与讲解

> 基于 Bruno Gavranović 等的论文 (ICML 2024, PMLR 235)

---

## 目录

1. [第2.3节：何去何从？](#sec23)
2. [第3节：2-范畴与参数化态射](#sec3)
   - [3.0 引言](#sec30)
   - [3.1 2-范畴 Para](#sec31)
   - [3.2 二维范畴代数](#sec32)
3. [总结](#summary)

---

<a name="sec23"></a>
## 1. 第2.3节：何去何从？(Where to Next?)

### 原文翻译

我们退一步，看看我们做了什么。我们已经展示了：一个现有的范畴论框架，能**统一地**捕捉各种不同的数据结构和自动机——只需将它们视为某个自函子（endofunctor）的特定（余）代数。通过选择一个我们已经理解透彻的数据结构，利用这些自函子（余）代数的同态，我们就能对相应神经网络的控制流施加结构化约束。这些约束和 monad 代数同态遵循完全相同的"配方"，因此可以被看作是对等变性的**推广**——描述了几何深度学习**之外**的函数。

---

这是对我们立场的具体证据——**范畴代数同态**非常适合捕捉可以施加在深度学习架构上的各种约束。到目前为止，我们的证据建立在**自函子代数**上，这是一个特别富有成果的变体。

---

然而，这个构造还有很多不足之处。一个主要问题是：为了定义任何形式的**权重共享**，在所有这些例子中，我们都隐含地假设同态是线性变换——通过把它们放在 **Vect** 范畴中。但大多数神经网络不仅仅是线性映射，这意味着这些分析仅限于分析它们单独的层。在循环神经网络的标准例子中，RNN 单元是一个**任意可微**函数，通常由一系列线性和非线性映射组成。此外，无论是在实践还是理论中，神经网络都被视为**参数化**函数。具体来说，在像 **JAX** 这样的框架中，参数通常需要被**显式地**传递给神经网络的任何前向或后向传播。

---

我们如何在不放弃已有的范畴框架的前提下，显式地建模参数和非线性映射？此外，在实践中——比如循环或递归神经网络——已经有成熟的权重共享技术。我们能否建立**形式化标准**来判断这些技术何时是**正确的**？正如我们将几何深度学习推广到范畴论的设定中一样，我们可以走得更远：进入 **2-范畴**的设定——我们用它来研究**参数化态射**。

---

### 讲解

这一节是一个**过渡段落**。它做了三件事：

1. **总结成就**：作者确认，用自函子的（余）代数+代数同态的方式，已经可以统一捕捉列表、树、Mealy 自动机等——这些都是"推广的等变性约束"。

2. **指出不足**：
   - 之前为了让"代数同态 = 线性层"，不得不把一切塞进 **Vect**（向量空间范畴），但这只能分析**单层**的线性映射。
   - 真正的神经网络（比如 RNN 单元）是**非线性的、参数化的**。
   - JAX 等框架里参数是**显式传递**的——当前框架没有建模"参数"。

3. **引出第3节**：要解决这个问题，需要进入 **2-范畴**，去研究**参数化态射**。

---

### 关键术语

| 术语 | 含义 |
|------|------|
| **权重共享 (weight sharing)** | 同一个权重矩阵在多个位置被重复使用（如 RNN 每一步用同一个 cell） |
| **参数化态射 (parametric morphisms)** | 把"参数"作为态射的一部分来建模 |
| **2-范畴 (2-category)** | 比普通范畴多一层结构——不仅对象之间有态射，态射之间还有"态射之间的态射"（2-态射） |

---

<a name="sec3"></a>
## 2. 第3节：2-范畴与参数化态射

<a name="sec30"></a>
### 2.1 引言 (3.0)

#### 原文翻译

尽管范畴论是一个强大的框架，但它在态射之间的**高阶关系**方面还有很多不足。它只处理态射的**集合**，没有办法**比较**这些集合中的元素。这就是 **2-范畴**理论的用武之地——它处理的是整个态射**范畴**。在（1-）范畴中，我们有对象和对象之间的态射；而在 2-范畴中，我们有对象（称为**0-态射**）、对象之间的态射（**1-态射**）、以及态射之间的态射（**2-态射**）。事实上，我们在定义范畴论的基本概念时，已经悄悄见过了 2-范畴的一个实例——**Cat**。具体来说，在 **Cat** 中，对象是**范畴**，态射是范畴之间的**函子**，而 2-态射是函子之间的**自然变换**。

> 要点： Cat 本身就是 2-范畴的原型实例——范畴、函子、自然变换 恰好对应 0-态射、1-态射、2-态射。

---

<a name="sec31"></a>
### 2.2 第3.1节：2-范畴 Para

#### 原文翻译

在本节中，我们将定义一个已知的 2-范畴 **Para**，然后详细阐述我们如何用它来形式化地建模权重共享。

---

虽然 **Para** 与 **Set** 范畴共享对象，但它的 1-态射不是普通函数，而是**参数化函数**。也就是说，这里的一个 1-态射 A → B 由一个对 (P, f) 组成，其中 P ∈ **Set**，而 f : P × A → B。

---

2-范畴 **Para** 建模了神经网络复合的代数结构；参数化态射的顺序复合会将参数空间**并行地**组合在一起。

---

**Para** 中的 2-态射捕捉了参数化函数之间的**重参数化**。重要的是，这允许显式地处理**权重绑定**——对于一个参数化态射 (P × P, f)，可以通过与复制映射 Δ_P : P → P × P 进行前复合来绑定其权重。

---

#### 讲解：Para 的核心思想

**Para vs 普通范畴：**

| 普通范畴（如 Set） | 2-范畴 Para |
|---|---|
| 对象 = 集合 | 对象 = 集合（和 Set 一样） |
| 态射 = 函数 f: A → B | 1-态射 = **参数化函数** (P, f)，其中 f: P × A → B |
| 没有态射之间的态射 | 2-态射 = **重参数化**（改变参数的方式） |

---

#### 为什么这很重要？

- 普通的神经网络层不是纯函数 A → B，而是**带参数**的函数 W × A → B（W 是权重）。
- Para 把参数 P 显式地作为态射的一部分：参数画在竖线（垂直方向）上，和数据流（水平方向）区分开。

  ```
      ┌───┐
      │ P │  ← 参数空间（垂直方向）
      └───┘
        │
  A ────┼──── B  ← 数据流（水平方向）
        │
        f
  ```

- 当两个参数化态射串行复合时，它们的参数空间**并行拼接**：

  ```
  (P, f) 然后 (Q, g)  →  (P × Q, g∘f)
  ```

---

#### 2-态射（重参数化）与权重绑定

- 2-态射描述"如何改变参数空间"。最经典的例子是 **weight tying**（权重绑定）：
  - 假设你有一个需要两个参数空间的态射 (P × P, f)
  - 但你希望两个参数"共享"——即每次用同一个 P
  - 这可以通过复制映射 Δ: P → P × P 实现：先把 P 复制成两份，再喂给 f
  - 效果就是你用 P 替代了 P × P，完成了"权重绑定"

```
       P          → 2-态射是"如何从 P 变成 P×P"
       │
       Δ (复制)
       │
    P × P         → 原态射需要两个参数
```

---

#### 对深度学习的具体意义

RNN 的每一步都用**同一个** cell（同一组权重），这就是典型的 weight tying。在 Para 框架下，这种"复用同一参数"的行为可以用 2-态射（复制映射 Δ）来形式化地描述和证明其正确性。

---

<a name="sec32"></a>
### 2.3 第3.2节：二维范畴代数

#### 原文翻译

2-范畴理论比 1-范畴理论丰富得多。在 1-范畴中，图表要么交换，要么不交换；而在 2-范畴中，图表只是一个**1-骨架**，2-态射附着其上。在任何 2-范畴中，一个方形可能：**交换**、**伪交换**、**lax-交换**、或 **oplax-交换**，分别意味着相关路径相等、同构、或存在从一个方向到另一个方向的 2-态射。

---

从长远来看，我们预期**所有这些**概念都将适用于解释或指定过去、现在和未来的神经架构的各个方面。先聚焦其中一个——**lax 代数**足以从第一性原理推导出**递归**、**循环**及类似的神经网络。

---

值得注意的是，lax 代数的态射也足以捕捉**1-上循环**，这被用来形式化异步神经网络（Dudzik et al., 2024）。

---

有趣的是，关于单个 RNN/递归神经网络单元如何生成完整的循环/递归神经网络的故事，正是我们在 Remark 2.13 中简略提到的**自函子上的代数自由 monad**故事的特定 2-范畴类比。

---

对于 2.2 节中的所有自函子例子，都存在一个 monad，其代数范畴 **Free_Mnd(F)** 等价于原始自函子 F 的代数范畴。我们通过**不断迭代 F 直到它稳定**来获得 **Free_Mnd(F)**，这意味着进一步应用该自函子不会改变复合结果。

---

但在二维情况下，我们研究 **Lax-Alg_Endo(F)** 和 **Lax-Alg_Mnd(Free_Mnd(F))** 之间的关系，不仅需要处理一维结构映射的生成，还需要处理 monad 的 lax 代数的 2-胞腔。

---

相反，我们关心的例子来自特定的 1-范畴代数（群作用 monad、归纳类型等），它们被**提升**为 **Para** 上的 2-monad。正如我们在定理 G.10 中证明的，这类代数的 lax 胞腔实际上是**余单子（comonoid）**。我们可以复制或删除向量中的条目——这正是权重绑定的本质——是这个余单子结构的非形式化面貌。

---

现在我们可以描述循环、递归及类似模型的普适性质了：它们是**由参数化自函子生成的自由参数化 monad 的 lax 代数**！将第 2 部分引入的代数概念提升到 2-范畴后，我们现在可以从函数式编程的第一性原理出发，**完整地**（而不仅仅是单独的层！）描述多种有影响力的神经网络。

---

#### 讲解

##### 2-范畴中的四种交换方式

在普通范畴中，一个方形图表里的两条路径要么相等（交换），要么不相等。2-范畴中多出了"宽松"的选项：

| 类型 | 符号 | 含义 |
|------|------|------|
| **严格交换** (commute) | `=` | 两条路径严格相等 |
| **伪交换** (pseudo-commute) | `≅` | 两条路径**同构**（存在可逆的 2-态射） |
| **lax-交换** | `⇒` | 存在一个方向的 2-态射（不可逆） |
| **oplax-交换** | `⇐` | 存在反方向的 2-态射 |

示意图（方形图表的四条边 + 中间的 2-态射）：

```
严格:     • ──── •        伪:      • ──── •
          │       │                 │       │
          │   =   │                 │   ≅   │
          │       │                 │       │
          • ──── •                 • ──── •

lax:      • ──── •        oplax:   • ──── •
          │       │                 │       │
          │   ⇒   │                 │   ⇐   │
          │       │                 │       │
          • ──── •                 • ──── •
```

---

##### 为什么 Lax 代数很重要？

作者强调 **lax 代数**是推导 RNN 等架构的关键工具。原因如下：

- 回想 2.2 节：列表是 `1 + A × (−)` 的代数，Mealy 机是 `I → O × (−)` 的余代数。
- 在 Para 中，这些自函子被"参数化"了——它们变成了 **参数化自函子**（parametric endofunctors）。
- **"自由 monad"**相当于把自函子不断迭代：
  - 列表 = Nil（空） | Cons（一个元素 + 另一个列表）
  - N 次迭代后自函子"稳定"不再变化
- 在二维版本中，不仅需要生成 1-态射，还需要生成 2-胞腔——而 lax 结构恰好提供了这种"宽松"的灵活性。

---

##### 🏆 核心结论

> **RNN、递归神经网络等模型的普适性质是：**
>
> **它们是由参数化自函子生成的自由参数化 monad 的 lax 代数。**

**翻译成人话（四步法）：**

| 步骤 | 操作 |
|------|------|
| ① | 先选一个数据类型（列表、树、Mealy 机……）对应的自函子 |
| ② | 把它变成"参数化"版本（放进 Para） |
| ③ | 取它的自由 monad（不断迭代直到稳定） |
| ④ | 看它的 lax 代数 → 自动得到对应的神经网络架构的**完整规范**（不只是单层！） |

**具体对应关系（来自论文 Figure 1）：**

| 自函子 F | 数据类型 | 神经网络架构 |
|----------|----------|-------------|
| `1 + A × (−)` | 列表 (List) | 折叠式 RNN (Folding RNN) |
| `A + (−)²` | 二叉树 (Tree) | 递归神经网络 (Recursive NN) |
| `I → O × (−)` | Mealy 机 | 完整 RNN (Full RNN / Unfolding RNN) |
| `O × (I → −)` | Moore 机 | Moore 机神经网络 |

---

##### 余单子（comonoid）与权重绑定

定理 G.10 说这些 lax 胞腔本质上是**余单子**。

余单子的核心操作：
- **复制** (copy)：`X → X ⊗ X`——一份变两份
- **删除** (delete)：`X → 1`——丢弃

这不就是"把同一份权重复制到多个位置使用"（weight tying）吗？

所以 **权重绑定在数学上被精确地刻画为余单子结构**——这不是 hack，而是有深层数学原理支撑的必然结果。

---

<a name="summary"></a>
## 3. 总结

### 第 2.3 节的论点

用自函子代数描述神经网络约束是有力的，但有一个致命缺陷——它只能分析**单层线性映射**，无法处理**参数化**和**非线性**。

### 第 3 节的解决方案

1. **引入 2-范畴 Para**：把参数显式建模为态射的一部分，用 2-态射建模重参数化和权重绑定。
2. **将概念提升到 2-范畴**：将 1-范畴中的自函子代数概念提升为 2-范畴中的概念，特别使用 **lax 代数**。
3. **核心结论**：
   > RNN 等架构的本质 = 参数化自函子的自由 monad 上的 lax 代数。
   >
   > 这是从函数式编程的第一性原理**唯一确定**的。

### 一句话记住这篇论文

> 选自函子 → 参数化 → 自由 monad → lax 代数 = 你的神经网络架构，自动带有正确的权重共享。